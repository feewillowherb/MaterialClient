# Force all text files to use LF line endings in Git repository
* text=auto eol=lf

# Explicitly specify script files must use LF (prevent Docker runtime failures)
*.sh text eol=lf
*.py text eol=lf

# Keep binary files as-is
*.png binary
*.jpg binary
